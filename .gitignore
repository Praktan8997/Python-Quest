# Python & Virtualenv
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
.venv

# Database
*.db
*.sqlite3

# Node & Frontend
node_modules/
dist/
dist-ssr/
*.local
.nx/

# Environment & Secrets — ignore actual secret files, but allow .env.example and .env.production
.env
.env.local
# Note: .env.production is intentionally committed (contains only public URLs, no secrets)
# Note: .env.example is intentionally committed (template for developers)

# IDE & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
